run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - whitespace

linters-settings:
  goimports:
    local-prefixes: net-cat

issues:
  max-same-issues: 0
